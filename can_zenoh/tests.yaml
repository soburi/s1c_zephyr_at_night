sample:
  name: CAN-Zenoh serial gateway
tests:
  sample.can_zenoh.gateway:
    tags:
      - can
      - zenoh
    depends_on:
      - can
      - gpio
    filter: dt_chosen_enabled("zephyr,canbus") and dt_alias_exists("zenoh-uart")
      and dt_alias_exists("led0")
    build_only: true
