sample:
  name: CAN communication with per-board IDs
tests:
  sample.can.peer:
    tags:
      - can
      - gpio
    depends_on:
      - can
      - gpio
    filter: dt_chosen_enabled("zephyr,canbus") and dt_alias_exists("sw0") and
      dt_alias_exists("led0")
    build_only: true
